# safety-service — module 2

Code : `app/main.py`, `app/routes.py`, `app/schemas.py`, `app/engine.py`.
Dix couches locales, NER français et pseudonymisation ; sortie nulle en cas de blocage.
Contrat : `openapi.json`. Compte PostgreSQL et Redis : `edu_safety`.

Les contrôles et leurs limites sont documentés dans
[`docs/module2.md`](../../docs/module2.md).
