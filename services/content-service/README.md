# content-service

Service FastAPI du module 3. Code complet dans `app/`, modèles Pydantic dans
`app/schemas.py`, contrat versionné `openapi.json`. Voir [guide](../../docs/module3.md).

Tests : `bash scripts/test_module3.sh`, dans une base jetable avec PostgreSQL et Redis réels.
